apiVersion: v1
kind: ServiceAccount
metadata:
  name: todo-api-sa
  namespace: testapp-2-defect
  annotations:
    azure.workload.identity/client-id: <APP-MIDEVOPSAGENT.CLIENTID>
    azure.workload.identity/use: "true"
